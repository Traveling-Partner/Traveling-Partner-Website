# Local: call backend directly (OK with http://localhost).
# Vercel: remove this var in Project → Settings → Environment Variables (or leave empty) so the app uses /website-api proxy and avoids mixed-content blocking.
NEXT_PUBLIC_API_BASE_URL=http://45.55.78.67:8080
