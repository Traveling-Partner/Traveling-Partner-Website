# Local http://localhost: direct backend URL is fine.
# On HTTPS (Vercel), the app uses /website automatically — this var is ignored in the browser there.
NEXT_PUBLIC_API_BASE_URL=https://api.traveling-partner.com/api
