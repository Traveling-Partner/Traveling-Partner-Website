# Local http://localhost: direct backend URL is fine.
# On HTTPS (Vercel), the app uses /website-api automatically — this var is ignored in the browser there.
# NEXT_PUBLIC_API_BASE_URL=http://45.55.78.67:8080
NEXT_PUBLIC_API_URL=https://api.traveling-partner.com/api