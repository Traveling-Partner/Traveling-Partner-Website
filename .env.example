# Live site URL — must match the domain you share (no trailing slash)
NEXT_PUBLIC_SITE_URL=https://traveling-partner.com

# API base (includes /api)
NEXT_PUBLIC_API_BASE_URL=https://api.traveling-partner.com/api
# Newsletter: POST {NEXT_PUBLIC_API_BASE_URL}/subscribers/subscribe  body: { "email" }

# Backward-compatible legacy base (without /api)
# NEXT_PUBLIC_API_BASE_URL=http://45.55.78.67:8080

# Optional: where next dev proxies /website (default matches backend)
# BACKEND_ORIGIN=http://45.55.78.67:8080

# Live Trip Sharing map (components/live-trip/LiveTripMap.tsx):
# - Without a key: real Google Maps via the keyless embed (works out of the
#   box, brand-tinted, but no moving vehicle marker).
# - With a key: full Maps JavaScript API — brand-styled tiles + live moving
#   vehicle. Requires a billing-enabled Google Cloud project (free tier
#   covers ~10,000 loads/month). Create a key at
#   https://developers.google.com/maps/documentation/javascript/get-api-key,
#   enable "Maps JavaScript API", restrict by HTTP referrer, then restart
#   the dev server (NEXT_PUBLIC_ vars are inlined at build/start time).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Vercel (HTTPS): if an HTTP base is set, the app falls back to
# /website/* → vercel.json proxies to Spring (avoids mixed content).

# Blog list/detail: https://api.traveling-partner.com/api/website/blog/list
# Staging browsers get 403 from that API (CORS) — app falls back to
# /blog-list.json generated at build from the same production URL.
